Follow README.md for build and test commands.

Never add API keys, credentials, generated run directories, or local Minecraft configs to Git.
Keep OpenAI calls client-only. Keep inventory mutation and plan validation server-side.
